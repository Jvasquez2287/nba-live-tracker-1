<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>

    <!-- Enable WebSockets -->
    <webSocket enabled="true" />

    <!-- IISNode handler - Plesk compatible -->
    <handlers>
      <add name="iisnode" path="dist/index.js" verb="*" modules="iisnode" resourceType="Unspecified" requireAccess="Script" />
    </handlers>

    <!-- URL Rewrite rules -->
    <rewrite>
      <rules>

        <!-- Allow static files if you ever add any -->
        <rule name="StaticFiles" stopProcessing="true">
          <match url="^.*\.(html|js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|map|json|txt|webmanifest)$" />
          <action type="None" />
        </rule>

        <!-- WebSocket endpoints -->
        <rule name="WebSocketMain" stopProcessing="true">
          <match url="^api/v1/ws$" />
          <action type="Rewrite" url="dist/index.js" />
        </rule>

        <rule name="WebSocketPlayByPlay" stopProcessing="true">
          <match url="^api/v1/playbyplay/ws/(.*)$" />
          <action type="Rewrite" url="dist/index.js" />
        </rule>

        <!-- API routes -->
        <rule name="APIRoutes" stopProcessing="true">
          <match url="^api/(.*)$" />
          <action type="Rewrite" url="dist/index.js" />
        </rule>

        <!-- Health check -->
        <rule name="HealthCheck" stopProcessing="true">
          <match url="^$" />
          <action type="Rewrite" url="dist/index.js" />
        </rule>

        <!-- Catch-all: everything goes to Node.js -->
        <rule name="NodeJS" stopProcessing="true">
          <match url="(.*)" />
          <action type="Rewrite" url="dist/index.js" />
        </rule>

      </rules>
    </rewrite>

    <!-- MIME types -->
    <staticContent>
      <mimeMap fileExtension=".json" mimeType="application/json" />
      <mimeMap fileExtension=".js" mimeType="application/javascript" />
      <mimeMap fileExtension=".css" mimeType="text/css" />
      <mimeMap fileExtension=".woff" mimeType="application/font-woff" />
      <mimeMap fileExtension=".woff2" mimeType="font/woff2" />
    </staticContent>

    <!-- Security headers -->
    <httpProtocol>
      <customHeaders>
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-Frame-Options" value="DENY" />
        <add name="X-XSS-Protection" value="1; mode=block" />
        <add name="Referrer-Policy" value="strict-origin-when-cross-origin" />
      </customHeaders>
    </httpProtocol>

    <!-- Pass errors to Node.js -->
    <httpErrors existingResponse="PassThrough" />

    <!-- IISNode settings - Plesk optimized -->
    <iisnode
      loggingEnabled="true"
      logDirectory="iisnode"
      devErrorsEnabled="false"
      nodeProcessCommandLine="node.exe"
      node_env="production"
      maxRequestSize="134217728"
      maxChildProcessesPerApplication="0"
      recycleSignalEnabled="true"
    />

    <!-- Default document -->
    <defaultDocument>
      <files>
        <clear />
        <add value="dist/index.js" />
      </files>
    </defaultDocument>

  </system.webServer>

  <appSettings>
    <add key="NODE_ENV" value="production" />
  </appSettings>

</configuration>